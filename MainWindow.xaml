<Window x:Class="WoodAccountingSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WoodAccountingSystem"
        mc:Ignorable="d"
        Title="Система учета древесины" Height="650" Width="800">
    <Grid Background="Chocolate">

        <Grid.RowDefinitions>
            <RowDefinition Height="17*"></RowDefinition>
            <RowDefinition Height="70*"></RowDefinition>
            <RowDefinition Height="13*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="DarkGray">
            <TextBlock Text="Система учета лесоматериалов" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="27" FontWeight="DemiBold"></TextBlock>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="15*"></ColumnDefinition>
                    <ColumnDefinition Width="70*"></ColumnDefinition>
                    <ColumnDefinition Width="15*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Frame Grid.Column="1" x:Name="FrameMain" NavigationUIVisibility="Hidden" Padding="0 20 0 20"></Frame>

            </Grid>
            
        </ScrollViewer>

        <Grid Grid.Row="2" Background="DarkGray"></Grid>
                
    </Grid>
</Window>
